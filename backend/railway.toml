[build]
builder = "NIXPACKS"
buildCommand = "npm ci --production=false && npm rebuild @swc/core --build-from-source && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10